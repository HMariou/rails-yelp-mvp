<h1>Look at those restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= restaurant.name%>
    <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary"%></li>
  <% end %>
</ul>

<%= link_to('Create a new restaurant!', new_restaurant_path)%>
